const HOME = {
  INDEX: "/",
};

const NEWS_LIST = {
  INDEX: "/newsList/:slug",
};

const NEWS_DETAIL = {
  INDEX: "/newsDetail/:id",
};

const CONTACT = {
  INDEX: "/contact",
};

const PHOTO_DETAIL = {
  INDEX: "/photoDetail/:id",
};

const AUDIO_DETAIL = {
  INDEX: "/audioDetail/:id",
};

const VIDEO_DETAIL = {
  INDEX: "/videoDetail/:id",
};

const AUDIO_LIST = {
  INDEX: "/audioList/:slug",
};

const PHOTO_LIST = {
  INDEX: "/photoList",
};

const VIDEO_LIST = {
  INDEX: "/videoList/:slug",
};

const TEAM = {
  INDEX: "/team",
};

const SCHEDULE = {
  INDEX: "/scheduleList/:id",
};

const LIVE = {
  INDEX: "/live",
};

const SEARCH = {
  INDEX: "/search/:keyword",
};

const TAG = {
  INDEX: "/tagList/:id",
}

const UNICODE = {
  INDEX: "/unicode",
};

const ABOUT = {
  INDEX: "/about",
};

const ERROR = {
  INDEX: "*",
};

export {
  HOME,
  NEWS_LIST,
  NEWS_DETAIL,
  CONTACT,
  AUDIO_DETAIL,
  AUDIO_LIST,
  VIDEO_DETAIL,
  VIDEO_LIST,
  PHOTO_LIST,
  PHOTO_DETAIL,
  TEAM,
  SCHEDULE,
  LIVE,
  SEARCH,
  TAG,
  UNICODE,
  ABOUT,
  ERROR,
};
